{
  "name": "agent-flow",
  "private": true,
  "description": "노드를 배치하여, 노코딩 기반으로 프로젝트를 구현할 수 있는 플랫폼",
  "scripts": {
    "dev": "npx concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "postinstall": "npm install --prefix server && npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
